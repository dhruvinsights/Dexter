# Core Dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
# Requires Python >= 3.10 on Apple Silicon (arm64 wheels ship from 3.10+; 3.9
# has no arm64 wheel and falls back to a source build needing the CLI driver).
# Note: ibm_db_dbi is bundled INSIDE the ibm-db wheel — not a separate package.
ibm-db>=3.2.0
ollama>=0.3.0
python-multipart>=0.0.6

# AI/ML
httpx>=0.24.0
aiohttp>=3.8.0
openai>=1.0.0
google-generativeai>=0.8.0

# API Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Utilities
requests>=2.31.0

# Document Processing (for RAG)
PyPDF2>=3.0.0
python-docx>=0.8.11

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0