import { SoundNames } from './sounds';

export class SoundManager {
  init(): void {
    // Audio disabled in OSS/dev build
  }

  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  play(_sound: SoundNames): void {
    // Audio disabled in OSS/dev build
  }

  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  stop(_sound: SoundNames): void {
    // Audio disabled in OSS/dev build
  }
}
