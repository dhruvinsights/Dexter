/**
 * Stub for the OSS build — the full Filter Menu plugin is a Pro feature.
 * Kept so the settings type reference in core-settings.ts resolves.
 */
export interface FilterPluginSettings {
  [key: string]: boolean | undefined;
}
